{
  "name": "speedshop-dotcom",
  "type": "module",
  "scripts": {
    "build": "node _scripts/build.mjs",
    "test": "npx --no-install playwright test",
    "dev:worker": "wrangler dev --config workers/wrangler.toml",
    "dev:static": "caddy run --config Caddyfile"
  },
  "dependencies": {
    "pjax": "^0.2.8",
    "quicklink": "^3.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "esbuild": "^0.28.1",
    "prettier": "^3.8.1",
    "purgecss": "^8.0.0",
    "wrangler": "^4.76.0"
  }
}
