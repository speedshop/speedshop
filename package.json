{
  "name": "speedshop-dotcom",
  "type": "module",
  "scripts": {
    "build": "node _scripts/build.mjs",
    "test": "npx playwright test"
  },
  "dependencies": {
    "pjax": "^0.2.8",
    "quicklink": "^3.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "esbuild": "^0.27.2"
  }
}
